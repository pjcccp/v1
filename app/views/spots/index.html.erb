
<h1>Listing spots</h1>

<p>
<table>
  <tr>
    <th>Title</th>
    <th>Name</th>
    <th>Amount</th>
    <th>Notes</th>
    <th>Payback date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @spots.each do |spot| %>
  <tr>
    <td><%= spot.title %></td>
    <td><%= spot.name %></td>
    <td><%= spot.amount %></td>
    <td><%= spot.notes %></td>
    <td><%= spot.payback_date %></td>
    <td><%= link_to 'Show', spot %></td>
    <td><%= link_to 'Edit', edit_spot_path(spot) %></td>
    <td><%= link_to 'Destroy', spot, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>
<p>
<br />

<%= link_to 'New Spot', new_spot_path %>
